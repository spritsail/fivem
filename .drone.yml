---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: build
  pull: always
  image: spritsail/docker-build

- name: publish
  pull: always
  image: spritsail/docker-publish
  settings:
    repo: spritsail/fivem
    tags:
    - stable
    login: {from_secret: docker_login}
  when:
    branch:
    - stable
    event:
    - push

---
kind: pipeline
name: update-readme

steps:
- name: dockerhub-readme
  pull: always
  image: jlesage/drone-push-readme
  settings:
    username: {from_secret: docker_username}
    password: {from_secret: docker_password}
    repo: spritsail/fivem
  when:
    branch:
    - stable
    event:
    - push

---
kind: signature
hmac: 75c00faa415b45d416042132f39baf20f4b5435fc7eac0eed8b3510f2c3aa7d0

...
